# ESP32-C6-DevKitC-1 (ESP32-C6-WROOM-1, 4 MB flash).
# Two OTA slots of 1.75 MB each so an update can never overwrite the running
# image, leaving ~192 KB for the stored configuration. The data partition must be
# labelled "spiffs": that is what Arduino's LittleFS::begin() looks for by
# default, and the other boards use the same name.
# Name,   Type, SubType, Offset,  Size,     Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1C0000,
app1,     app,  ota_1,   0x1D0000,0x1C0000,
spiffs,   data, spiffs,  0x390000,0x30000,
coredump, data, coredump,0x3C0000,0x10000,
